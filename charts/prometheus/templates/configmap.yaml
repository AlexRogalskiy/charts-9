kind: ConfigMap
apiVersion: v1
metadata:
  name: {{ template "fullname" . }}
data:
  prometheus.yml: >
{{ .Files.Get "prometheus.yml" | indent 4 }}
  rules: >
{{ .Values.rules | indent 4 }}
  alerts: >
{{ .Values.alerts | indent 4 }}
